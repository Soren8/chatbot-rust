# MANDATORY: PROJECT CONTEXT & INSTRUCTIONS

You MUST read `AGENTS.md` and, if it exists, `~/.grok/AGENTS.md` before performing any task. This is non-negotiable.

`AGENTS.md` (this repo) contains:
- Crucial build and run commands (Docker `tests` service only; no live-stack compose).
- Code style and error handling guidelines.
- Mandatory directory structure for logs and caches.
- Product constraints (TTS, secrets, provider config).

`~/.grok/AGENTS.md` (user-level, if present) contains sandbox, scope, git, and working-style policy.

Do not proceed without first reading those files.
