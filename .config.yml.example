llms:
  - name: "Local Ollama (Free Tier)"
    type: "ollama"
    base_url: "http://${OLLAMA_HOST}:${OLLAMA_PORT}"
    model_name: "dolphin3.1-8b"
    context_size: 8192
    tier: "free"
    template: |
      {% if system_prompt %}
      <|im_start|>system
      {{ system_prompt }}<|im_end|>
      {% endif %}

  - name: "GPT-4 (Premium)"
    type: "openai" 
    api_key: "${OPENAI_API_KEY}"
    model_name: "gpt-4-turbo"
    tier: "premium"

default_llm: "Local Ollama (Free Tier)"
session_timeout: 3600
